#include <stdio.h>
#include <conio.h> 
void main(int) 
 {
	int number,index;
	scanf("%d",&number);
	int a[number];
	for(index=0;index<n;index++)
	scanf("%d",&a[index]);
	for(index=0;index<n;index++)
	{
		if(a[index]==i)
		printf("%d",a[index]);
	}
}
